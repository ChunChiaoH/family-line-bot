Scaffold a new tool for the bot following the project's existing patterns:
1. Create the tool function in `family_line_bot/services/`
2. Register it in `ClaudeService` with appropriate tags
3. Wire it up in `family_line_bot/app.py`
4. Update `CLAUDE.md` if the tool introduces new patterns

Ask the user what the tool should do before starting.
